<style>
  .reader-container {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 20px;
  }
  @media (max-width: 900px) { .reader-container { grid-template-columns: 1fr; } }
  .reader-sidebar {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 16px;
    max-height: 85vh;
    overflow-y: auto;
    position: sticky;
    top: 80px;
  }
  .reader-main {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 10px;
    min-height: 85vh;
  }
  #pdfCanvas {
    display: block;
    margin: 0 auto;
    max-width: 100%;
    height: auto;
    box-shadow: 0 0 15px rgba(0,0,0,0.08);
  }
  .reader-toolbar {
    display: flex;
    gap: 8px;
    align-items: center;
    justify-content: center;
    padding: 10px;
    background: var(--bg);
    border-radius: 8px;
    margin-bottom: 10px;
    flex-wrap: wrap;
  }
  .reader-toolbar button { padding: 6px 12px; font-size: 0.9rem; }
  #pageInfo { font-weight: 600; min-width: 100px; text-align: center; }
</style>

<div class="page-header">
  <div>
    <a href="/buku/<%= buku.id %>" class="text-muted">&larr; Kembali ke Detail</a>
    <h1 style="margin:4px 0;"><%= buku.judul %></h1>
    <p class="subtitle">Baca online &middot; <%= buku.penulis %></p>
  </div>
</div>

<div class="reader-container">
  <aside class="reader-sidebar">
    <h4 style="margin-top:0">Bookmark</h4>
    <form method="POST" action="/murid/bookmark/<%= buku.id %>">
      <input type="hidden" name="halaman" id="bmHalaman" />
      <div class="form-group">
        <label>Catatan (opsional)</label>
        <textarea name="catatan" class="form-control" rows="2" placeholder="Tulis catatan untuk halaman ini"></textarea>
      </div>
      <button class="btn btn-primary btn-block" onclick="document.getElementById('bmHalaman').value = currentPage;">+ Simpan Bookmark Hal. Ini</button>
    </form>

    <h5 class="mt-3">Bookmark Saya</h5>
    <% if (bookmarks.length === 0) { %>
      <p class="text-muted" style="font-size:0.85rem;">Belum ada bookmark.</p>
    <% } %>
    <% bookmarks.forEach(bm => { %>
      <div style="padding:8px 0; border-bottom:1px solid var(--border);">
        <a href="#" onclick="goToPage(<%= bm.halaman %>); return false;" style="font-weight:600;">
          Hal. <%= bm.halaman %>
        </a>
        <% if (bm.catatan) { %>
          <p class="text-muted" style="margin:4px 0; font-size:0.82rem;"><%= bm.catatan %></p>
        <% } %>
        <form method="POST" action="/murid/bookmark/<%= bm.id %>?_method=DELETE" style="display:inline;">
          <button class="btn btn-sm btn-danger" style="padding:2px 8px;">X</button>
        </form>
      </div>
    <% }) %>

    <h5 class="mt-3">Progress</h5>
    <div class="progress"><div class="bar" id="progressBar" style="width: 0%"></div></div>
    <p class="text-muted mt-1" style="font-size:0.85rem;" id="progressText">0%</p>
  </aside>

  <main class="reader-main">
    <% if (pdfMode === 'iframe') { %>
      <iframe src="<%= pdfUrl %>" width="100%" height="900" style="border:0; border-radius:8px;" allow="autoplay"></iframe>
      <p class="text-muted text-center mt-2" style="font-size:0.85rem;">
        File disajikan via Google Drive Preview. Bookmark tidak tersedia untuk mode ini.
      </p>
    <% } else if (pdfMode === 'pdfjs') { %>
      <div class="reader-toolbar">
        <button class="btn btn-secondary" onclick="prevPage()">&lsaquo; Prev</button>
        <span id="pageInfo">Memuat...</span>
        <button class="btn btn-secondary" onclick="nextPage()">Next &rsaquo;</button>
        <button class="btn btn-secondary" onclick="zoomOut()">-</button>
        <button class="btn btn-secondary" onclick="zoomIn()">+</button>
        <input type="number" id="pageGoto" min="1" value="1" style="width:70px; padding:4px;" />
        <button class="btn btn-secondary" onclick="goToPage(parseInt(document.getElementById('pageGoto').value))">Go</button>
        <a href="<%= pdfUrl %>" download class="btn btn-secondary">Download</a>
      </div>
      <canvas id="pdfCanvas"></canvas>

      <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
      <script>
        pdfjsLib.GlobalWorkerOptions.workerSrc = 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';

        const bukuId = <%= buku.id %>;
        const pdfUrl = <%- JSON.stringify(pdfUrl) %>;
        const initialPage = <%= initialPage %>;
        let pdfDoc = null, currentPage = initialPage, scale = 1.3, totalPages = 0;
        const canvas = document.getElementById('pdfCanvas');
        const ctx = canvas.getContext('2d');

        pdfjsLib.getDocument(pdfUrl).promise.then(doc => {
          pdfDoc = doc;
          totalPages = doc.numPages;
          if (currentPage > totalPages) currentPage = totalPages;
          renderPage(currentPage);
        }).catch(err => {
          document.getElementById('pageInfo').innerText = 'Gagal memuat PDF';
          console.error(err);
        });

        function renderPage(num) {
          pdfDoc.getPage(num).then(page => {
            const viewport = page.getViewport({ scale });
            canvas.width = viewport.width;
            canvas.height = viewport.height;
            page.render({ canvasContext: ctx, viewport });
            document.getElementById('pageInfo').innerText = `Hal. ${num} / ${totalPages}`;
            const pct = Math.round((num / totalPages) * 100);
            document.getElementById('progressBar').style.width = pct + '%';
            document.getElementById('progressText').innerText = pct + '%';
            // Auto-save progress
            fetch('/murid/progress/reader', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ buku_id: bukuId, halaman: num, total: totalPages }),
            }).catch(() => {});
          });
        }
        function prevPage() { if (currentPage > 1) { currentPage--; renderPage(currentPage); } }
        function nextPage() { if (currentPage < totalPages) { currentPage++; renderPage(currentPage); } }
        function goToPage(n) { if (n >= 1 && n <= totalPages) { currentPage = n; renderPage(n); } }
        function zoomIn() { scale = Math.min(3, scale + 0.2); renderPage(currentPage); }
        function zoomOut() { scale = Math.max(0.5, scale - 0.2); renderPage(currentPage); }
        document.addEventListener('keydown', (e) => {
          if (e.key === 'ArrowLeft') prevPage();
          if (e.key === 'ArrowRight') nextPage();
        });
      </script>
    <% } else { %>
      <div class="text-center" style="padding:60px 20px;">
        <h3>Buku ini belum memiliki file digital</h3>
        <p class="text-muted">Silakan pinjam versi fisik melalui menu "Pinjam Buku".</p>
        <a href="/buku/<%= buku.id %>" class="btn btn-primary">Kembali ke Detail</a>
      </div>
    <% } %>
  </main>
</div>
